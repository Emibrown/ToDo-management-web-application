import path from 'path';

export const DATA_PATH = path.join(process.cwd(),'src', 'infrastructure', 'data', 'todos.json');
export const DATA_PATH_USERS = path.join(process.cwd(), 'src', 'infrastructure', 'data', 'users.json');
export const SESSIONS_FILE = path.join(process.cwd(), 'src', 'infrastructure', 'data', 'sessions.json');